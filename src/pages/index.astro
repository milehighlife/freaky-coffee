---
import logo from '../assets/logo_large.png?url';
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Freaky Coffee | Weirdly Good Beans</title>
    <meta name="description" content="Coffee for night owls, punk rockers, and freaks everywhere. Freaky Coffee brews bold flavor with good vibes. Sip responsibly. Froth mischievously." />
    <style>
      :root { --bg: #4e02a5; --logo-glow: rgba(255,225,0,0.25); }
      html, body { height: 100%; }
      body {
        margin: 0;
        /* Use the small viewport unit for most accurate mobile height, fall back to dynamic viewport and then classic 100vh. */
        min-height: 100svh;
        height: 100dvh;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--bg);
        -webkit-font-smoothing: antialiased;
      }
      /* Move safe-area padding into `main` so the flex container (`body`) centers
         against the true viewport. This avoids centering shifts between Safari
         and Chrome caused by body padding reducing the flex container's content box. */
      main {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        /* Ensure `main` fills the viewport while accounting for mobile UI chrome. */
        min-height: 100svh;
        height: 100dvh;
        min-height: 100vh;
        box-sizing: border-box;
        padding-top: env(safe-area-inset-top);
        padding-bottom: env(safe-area-inset-bottom);
      }
      .logo {
        filter: drop-shadow(0 0 24px var(--logo-glow));
        animation: float 4s ease-in-out infinite;
        width: auto;
        max-width: min(70vw, 480px);
        height: auto;
        display: block;
      }
      @keyframes float {
        0% { transform: scale(1) rotate(0deg); }
        50% { transform: scale(1.15) rotate(-1deg); }
        100% { transform: scale(1) rotate(0deg); }
      }
    </style>
  </head>
  <body>
    <main>
      <img src={logo} alt="Freaky Coffee Logo" class="logo" />
    </main>
  </body>
</html>