---
import logo from '../assets/logo_large.png?url';
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Freaky Coffee | Weirdly Good Beans</title>
    <meta name="description" content="Coffee for night owls, punk rockers, and freaks everywhere. Freaky Coffee brews bold flavor with good vibes. Sip responsibly. Froth mischievously." />
    <style>
      :root { --bg: #4e02a5; --logo-glow: rgba(255,225,0,0.25); }
      html, body { height: 100%; }
      body {
        margin: 0;
        /* Use the small viewport unit for most accurate mobile height, fall back to dynamic viewport and then classic 100vh. */
        min-height: 100svh;
        height: 100dvh;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--bg);
        -webkit-font-smoothing: antialiased;
      }
      /* Use a single full-viewport grid container that owns centering and
         safe-area insets. Grid `place-items: center` is robust across
         browsers and avoids subtle flex/content-box differences that can
         shift visual centering on iOS Chrome. */
      main {
        display: grid;
        place-items: center;
        width: 100%;
        /* Prefer the small viewport height, then dynamic, then classic 100vh. */
        min-height: 100svh;
        height: 100dvh;
        min-height: 100vh;
        box-sizing: border-box;
        /* Let `main` absorb safe-area insets so the centering math is consistent. */
        padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left);
      }
      .logo {
        filter: drop-shadow(0 0 24px var(--logo-glow));
        animation: float 4s ease-in-out infinite;
        width: auto;
        max-width: min(70vw, 480px);
        height: auto;
        display: block; /* ensure no inline baseline spacing */
        margin: 0; /* be explicit so no user-agent margins affect centering */
      }
      @keyframes float {
        0% { transform: scale(1) rotate(0deg); }
        50% { transform: scale(1.15) rotate(-1deg); }
        100% { transform: scale(1) rotate(0deg); }
      }
    </style>
  </head>
  <body>
    <main>
      <img src={logo} alt="Freaky Coffee Logo" class="logo" />
    </main>
  </body>
</html>